name: iOS CI

on:
  push:
    branches: [ main, MovieDetailRefactor ]

jobs:
  build:
    runs-on: macos-14

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build app
        run: |
          xcodebuild clean build \
          -project MovieProject.xcodeproj \
          -scheme MovieProject \
          -destination 'platform=iOS Simulator,name=iPhone 15'
